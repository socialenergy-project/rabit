<%= content_for :title, "Alert #{@message.id}" -%>

<div class="row">
  <div class="col-lg-12">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        alert properties
      </div>
      <div class="card-body">
        <!--<p>
          <strong>Recommendation:</strong>
          <%= link_to @message.recommendation&.id, recommendation_path(@message.recommendation) if @message.recommendation -%>
        </p>-->
        <!--<p>
          <strong>Sender:</strong>
          <%= link_to @message.sender&.name, user_path(@message.sender) if @message.sender -%>
        </p>-->
        <p>
          <strong>Recipient:</strong>
          <%= link_to @message.recipient&.name, user_path(@message.recipient) if @message.recipient -%>
        </p>
        <p>
          <strong>Content:</strong>
          <%= @message.content-%>
        </p>
        <p>
          <strong>Status:</strong>
          <%= @message.status-%>
        </p>
        <p>
          <strong>Status:</strong>
          <%= @message.gsrn_status-%>
        </p>

        <%= link_to 'Edit', edit_message_path(@message), class: 'btn btn-warning btn-sm' if can? :edit, @message %> |
        <%= link_to 'All Alerts', messages_path, class: 'btn btn-info btn-sm' %>

      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>

