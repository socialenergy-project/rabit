<%= content_for :title, 'Editing Smart Plug' %>

<div class="row">
  <div class="col-lg-12">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        smart_plug properties
      </div>
      <div class="card-body">
        <%= render 'form', smart_plug: @smart_plug %>

        <%= link_to 'Show', @smart_plug, class: 'btn btn-info btn-sm' %> |
        <%= link_to 'All Smart Plugs', smart_plugs_path, class: 'btn btn-info btn-sm' %>
      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>
