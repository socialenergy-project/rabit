<%= content_for :title, 'Connection Type' %>

<div class="row">
  <div class="col-lg-12">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        connection_type properties
      </div>
      <div class="card-body">
                    <p>
              <strong>Name:</strong>
              
                  <%= @connection_type.name-%>
              

            </p>

        
        <%= link_to 'Edit', edit_connection_type_path(@connection_type), class: 'btn btn-warning btn-sm' if can? :edit, @connection_type %> |
        <%= link_to 'All Connection Types', connection_types_path, class: 'btn btn-info btn-sm' %>

      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>

