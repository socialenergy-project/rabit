<%= form_with(model: connection_type, local: true) do |form| %>
  <% if connection_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(connection_type.errors.count, "error") %> prohibited this connection_type from being saved:</h2>

      <ul>
      <% connection_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :connection_type_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.all, :id, :name, prompt: true  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
