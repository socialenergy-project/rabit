<%= content_for :title, 'Editing Cluster' %>

<div class="row">
  <div class="col-lg-8">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        cluster properties
      </div>
      <div class="card-body">
        <%= render 'form', cluster: @cluster %>

        <%= link_to 'Show', @cluster, class: 'btn btn-info btn-sm' %> |
        <%= link_to 'All Clusters', clusters_path, class: 'btn btn-info btn-sm' %>
      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>
