<%= form_with(model: consumer_category, local: true) do |form| %>
  <% if consumer_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consumer_category.errors.count, "error") %> prohibited this consumer_category from being saved:</h2>

      <ul>
      <% consumer_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.text_field :name, id: :consumer_category_name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :description, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.text_area :description, id: :consumer_category_description, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :real_time, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.check_box :real_time, id: :consumer_category_real_time, class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary btn-sm" %>
  </div>
<% end %>
