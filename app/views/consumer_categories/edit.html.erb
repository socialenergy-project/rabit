<%= content_for :title, 'Editing Consumer Category' %>

<div class="row">
  <div class="col-lg-12">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        consumer_category properties
      </div>
      <div class="card-body">
        <%= render 'form', consumer_category: @consumer_category %>

        <%= link_to 'Show', @consumer_category, class: 'btn btn-info btn-sm' %> |
        <%= link_to 'All Consumer Categories', consumer_categories_path, class: 'btn btn-info btn-sm' %>
      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>
