<%= content_for :title, 'Editing Recommendation' %>

<div class="row">
  <div class="col-lg-12">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        recommendation properties
      </div>
      <div class="card-body">
        <%= render 'form', recommendation: @recommendation %>

        <%= link_to 'Show', @recommendation, class: 'btn btn-info btn-sm' %> |
        <%= link_to 'All Recommendations', recommendations_path, class: 'btn btn-info btn-sm' %>
      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>
