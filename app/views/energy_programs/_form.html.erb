<%= form_with(model: energy_program, local: true) do |form| %>
    <% if energy_program.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(energy_program.errors.count, "error") %> prohibited this energy_program from being
            saved:</h2>

          <ul>
            <% energy_program.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group row">
      <%= form.label :name, class: "col-sm-3 col-form-label" %>
      <div class="col-sm-9">
        <%= form.text_field :name, id: :energy_program_name, class: "form-control" %>
      </div>
    </div>

    <div class="actions">
      <%= form.submit class: "btn btn-primary btn-sm" %>
    </div>
<% end %>
