<%= form_with(model: recommendation_type, local: true) do |form| %>
  <% if recommendation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recommendation_type.errors.count, "error") %> prohibited this recommendation_type from being saved:</h2>

      <ul>
      <% recommendation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.text_field :name, id: :recommendation_type_name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :description, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.text_area :description, id: :recommendation_type_description, class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary btn-sm" %>
  </div>
<% end %>
