<%= content_for :title, 'Editing Recommendation Type' %>

<div class="row">
  <div class="col-lg-12">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        recommendation_type properties
      </div>
      <div class="card-body">
        <%= render 'form', recommendation_type: @recommendation_type %>

        <%= link_to 'Show', @recommendation_type, class: 'btn btn-info btn-sm' %> |
        <%= link_to 'All Recommendation Types', recommendation_types_path, class: 'btn btn-info btn-sm' %>
      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>
