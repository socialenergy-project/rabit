<%= form_with(model: interval, local: true) do |form| %>
  <% if interval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interval.errors.count, "error") %> prohibited this interval from being saved:</h2>

      <ul>
      <% interval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.text_field :name, id: :interval_name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :duration, class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= form.number_field :duration, id: :interval_duration, class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary btn-sm" %>
  </div>
<% end %>
