<%= content_for :title, 'New Prosumer' %>

<div class="row">
  <div class="col-lg-8">
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-area-chart"></i>
        prosumer properties
      </div>
      <div class="card-body">
        <%= render 'form', prosumer: @prosumer %>

        <%= link_to 'All Prosumers', prosumers_path, class: 'btn btn-info btn-sm' %>
      </div>
      <div class="card-footer small text-muted">
      </div>
    </div>
  </div>
</div>
